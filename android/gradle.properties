org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# AGP 9's new DSL is opt-out for now; this project still uses the Groovy DSL.
android.newDsl=false
# Built-in Kotlin stays off until AGP ships a Kotlin new enough for Flutter.
# See the note in settings.gradle.
android.builtInKotlin=false
